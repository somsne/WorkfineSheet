# 📝 P0.2 单元格边框开发计划

> **功能**: 单元格边框样式系统  
> **优先级**: P0 (核心功能)  
> **预计时间**: 3 天  
> **依赖**: P0.1 单元格样式 ✅  
> **状态**: 🟡 进行中

---

## 🎯 功能目标

### 核心需求
1. ✅ **边框数据结构** - 支持四边独立设置
2. ✅ **边框样式** - 实线、虚线、点线、双线、粗线等
3. ✅ **边框属性** - 颜色、宽度可自定义
4. ✅ **Canvas 渲染** - 高性能边框绘制
5. ✅ **API 接口** - 完整的边框操作 API
6. ✅ **快捷方法** - 全边框、外边框、无边框

### 设计原则
- 🎯 **独立性** - 每条边可独立设置样式
- 🎯 **兼容性** - 与 Excel 边框样式对齐
- 🎯 **性能** - 优化重叠边框绘制
- 🎯 **扩展性** - 预留图案填充等高级功能

---

## 📐 数据结构设计

### 1. 边框样式枚举

```typescript
/**
 * 边框线型
 */
export type BorderStyle = 
  | 'none'        // 无边框
  | 'thin'        // 细线 (1px)
  | 'medium'      // 中等线 (2px)
  | 'thick'       // 粗线 (3px)
  | 'dashed'      // 虚线
  | 'dotted'      // 点线
  | 'double'      // 双线

/**
 * 单条边框配置
 */
export interface BorderEdge {
  style: BorderStyle
  color?: string  // 默认 #000000
  width?: number  // 自定义宽度，覆盖 style 预设
}

/**
 * 单元格四边边框
 */
export interface CellBorder {
  top?: BorderEdge
  right?: BorderEdge
  bottom?: BorderEdge
  left?: BorderEdge
}
```

### 2. 预设边框样式

```typescript
/**
 * 边框预设配置
 */
export const BORDER_PRESETS = {
  thin: { width: 1, pattern: [] },
  medium: { width: 2, pattern: [] },
  thick: { width: 3, pattern: [] },
  dashed: { width: 1, pattern: [4, 2] },
  dotted: { width: 1, pattern: [1, 1] },
  double: { width: 3, pattern: [] } // 双线需特殊处理
} as const

/**
 * 默认边框样式
 */
export const DEFAULT_BORDER_EDGE: Required<BorderEdge> = {
  style: 'thin',
  color: '#000000',
  width: 1
}
```

---

## 🎨 渲染实现

### 1. 边框绘制函数

```typescript
/**
 * 绘制单元格边框
 * 
 * @param ctx Canvas 上下文
 * @param x 单元格 X 坐标
 * @param y 单元格 Y 坐标
 * @param width 单元格宽度
 * @param height 单元格高度
 * @param border 边框配置
 */
function drawCellBorder(
  ctx: CanvasRenderingContext2D,
  x: number,
  y: number,
  width: number,
  height: number,
  border: CellBorder
): void {
  if (!border) return

  // 绘制顺序：top -> right -> bottom -> left
  // 这样可以保证边角连接正确
  
  if (border.top) {
    drawBorderEdge(ctx, x, y, x + width, y, border.top, 'horizontal')
  }
  
  if (border.right) {
    drawBorderEdge(ctx, x + width, y, x + width, y + height, border.right, 'vertical')
  }
  
  if (border.bottom) {
    drawBorderEdge(ctx, x, y + height, x + width, y + height, border.bottom, 'horizontal')
  }
  
  if (border.left) {
    drawBorderEdge(ctx, x, y, x, y + height, border.left, 'vertical')
  }
}

/**
 * 绘制单条边框
 */
function drawBorderEdge(
  ctx: CanvasRenderingContext2D,
  x1: number,
  y1: number,
  x2: number,
  y2: number,
  edge: BorderEdge,
  direction: 'horizontal' | 'vertical'
): void {
  if (edge.style === 'none') return

  const preset = BORDER_PRESETS[edge.style]
  const width = edge.width ?? preset.width
  const color = edge.color ?? '#000000'

  ctx.save()
  ctx.strokeStyle = color
  ctx.lineWidth = width

  // 设置虚线样式
  if (preset.pattern.length > 0) {
    ctx.setLineDash(preset.pattern)
  }

  // 双线特殊处理
  if (edge.style === 'double') {
    drawDoubleBorder(ctx, x1, y1, x2, y2, width, direction)
  } else {
    ctx.beginPath()
    ctx.moveTo(x1, y1)
    ctx.lineTo(x2, y2)
    ctx.stroke()
  }

  ctx.restore()
}

/**
 * 绘制双线边框
 */
function drawDoubleBorder(
  ctx: CanvasRenderingContext2D,
  x1: number,
  y1: number,
  x2: number,
  y2: number,
  width: number,
  direction: 'horizontal' | 'vertical'
): void {
  const gap = 1 // 双线间隙
  const lineWidth = Math.max(1, Math.floor((width - gap) / 2))
  
  ctx.lineWidth = lineWidth

  if (direction === 'horizontal') {
    // 上线
    ctx.beginPath()
    ctx.moveTo(x1, y1 - gap / 2 - lineWidth / 2)
    ctx.lineTo(x2, y2 - gap / 2 - lineWidth / 2)
    ctx.stroke()
    
    // 下线
    ctx.beginPath()
    ctx.moveTo(x1, y1 + gap / 2 + lineWidth / 2)
    ctx.lineTo(x2, y2 + gap / 2 + lineWidth / 2)
    ctx.stroke()
  } else {
    // 左线
    ctx.beginPath()
    ctx.moveTo(x1 - gap / 2 - lineWidth / 2, y1)
    ctx.lineTo(x2 - gap / 2 - lineWidth / 2, y2)
    ctx.stroke()
    
    // 右线
    ctx.beginPath()
    ctx.moveTo(x1 + gap / 2 + lineWidth / 2, y1)
    ctx.lineTo(x2 + gap / 2 + lineWidth / 2, y2)
    ctx.stroke()
  }
}
```

### 2. 集成到 renderCells

```typescript
// 在 drawCells 函数中，绘制单元格内容后绘制边框
export function drawCells(ctx: CanvasRenderingContext2D, config: CellsRenderConfig) {
  // ... 现有代码 ...

  // 遍历可见单元格
  for (let r = startRow; r <= endRow; r++) {
    for (let c = startCol; c <= endCol; c++) {
      // ... 绘制单元格内容 ...
      
      // 绘制边框（在内容之后，确保边框在最上层）
      const border = config.model.getCellBorder(r, c)
      if (border) {
        drawCellBorder(ctx, cellX, cellY, colWidth, rowHeight, border)
      }
    }
  }
}
```

---

## 🔌 API 设计

### 1. SheetModel 扩展

```typescript
export class SheetModel {
  // 边框存储
  private cellBorders: Map<string, CellBorder> = new Map()

  /**
   * 设置单元格边框
   */
  setCellBorder(row: number, col: number, border: Partial<CellBorder>): void {
    const key = `${row},${col}`
    const current = this.cellBorders.get(key) || {}
    this.cellBorders.set(key, { ...current, ...border })
  }

  /**
   * 获取单元格边框
   */
  getCellBorder(row: number, col: number): CellBorder | undefined {
    const key = `${row},${col}`
    return this.cellBorders.get(key)
  }

  /**
   * 清除单元格边框
   */
  clearCellBorder(row: number, col: number): void {
    const key = `${row},${col}`
    this.cellBorders.delete(key)
  }

  /**
   * 设置范围边框
   */
  setRangeBorder(
    startRow: number,
    startCol: number,
    endRow: number,
    endCol: number,
    border: Partial<CellBorder>
  ): void {
    for (let r = startRow; r <= endRow; r++) {
      for (let c = startCol; c <= endCol; c++) {
        this.setCellBorder(r, c, border)
      }
    }
  }

  /**
   * 设置范围外边框（只设置最外层四边）
   */
  setRangeOuterBorder(
    startRow: number,
    startCol: number,
    endRow: number,
    endCol: number,
    edge: BorderEdge
  ): void {
    for (let c = startCol; c <= endCol; c++) {
      // 上边框
      this.setCellBorder(startRow, c, { top: edge })
      // 下边框
      this.setCellBorder(endRow, c, { bottom: edge })
    }
    
    for (let r = startRow; r <= endRow; r++) {
      // 左边框
      this.setCellBorder(r, startCol, { left: edge })
      // 右边框
      this.setCellBorder(r, endCol, { right: edge })
    }
  }

  /**
   * 清除范围边框
   */
  clearRangeBorder(
    startRow: number,
    startCol: number,
    endRow: number,
    endCol: number
  ): void {
    for (let r = startRow; r <= endRow; r++) {
      for (let c = startCol; c <= endCol; c++) {
        this.clearCellBorder(r, c)
      }
    }
  }
}
```

### 2. 公共 API (api.ts)

```typescript
export interface SheetAPI {
  // ... 现有 API ...

  // 边框 API
  border: {
    /**
     * 设置单元格边框
     */
    setCellBorder(row: number, col: number, border: Partial<CellBorder>): void

    /**
     * 获取单元格边框
     */
    getCellBorder(row: number, col: number): CellBorder | undefined

    /**
     * 清除单元格边框
     */
    clearCellBorder(row: number, col: number): void

    /**
     * 设置范围边框（所有单元格全边框）
     */
    setRangeBorder(
      startRow: number,
      startCol: number,
      endRow: number,
      endCol: number,
      border: Partial<CellBorder>
    ): void

    /**
     * 设置范围外边框（只设置最外层）
     */
    setRangeOuterBorder(
      startRow: number,
      startCol: number,
      endRow: number,
      endCol: number,
      edge: BorderEdge
    ): void

    /**
     * 清除范围边框
     */
    clearRangeBorder(
      startRow: number,
      startCol: number,
      endRow: number,
      endCol: number
    ): void

    // 快捷方法
    /**
     * 设置全边框（所有单元格四边都有边框）
     */
    setAllBorders(
      startRow: number,
      startCol: number,
      endRow: number,
      endCol: number,
      edge: BorderEdge
    ): void

    /**
     * 设置外边框（只有外围有边框）
     */
    setOuterBorder(
      startRow: number,
      startCol: number,
      endRow: number,
      endCol: number,
      edge: BorderEdge
    ): void

    /**
     * 清除所有边框
     */
    clearAllBorders(
      startRow: number,
      startCol: number,
      endRow: number,
      endCol: number
    ): void
  }
}
```

### 3. 快捷方法实现

```typescript
// 在 api.ts 中实现快捷方法
function setAllBorders(
  startRow: number,
  startCol: number,
  endRow: number,
  endCol: number,
  edge: BorderEdge
): void {
  const border: CellBorder = {
    top: edge,
    right: edge,
    bottom: edge,
    left: edge
  }
  
  model.setRangeBorder(startRow, startCol, endRow, endCol, border)
  redraw()
}

function setOuterBorder(
  startRow: number,
  startCol: number,
  endRow: number,
  endCol: number,
  edge: BorderEdge
): void {
  model.setRangeOuterBorder(startRow, startCol, endRow, endCol, edge)
  redraw()
}

function clearAllBorders(
  startRow: number,
  startCol: number,
  endRow: number,
  endCol: number
): void {
  model.clearRangeBorder(startRow, startCol, endRow, endCol)
  redraw()
}
```

---

## 🧪 测试计划

### 1. 单元测试 (tests/borders.spec.ts)

```typescript
import { describe, it, expect } from 'vitest'
import { SheetModel } from '@/lib/SheetModel'
import type { CellBorder, BorderEdge } from '../types'

describe('单元格边框', () => {
  describe('数据结构', () => {
    it('应该设置和获取单元格边框', () => {
      const model = new SheetModel()
      const border: CellBorder = {
        top: { style: 'thin', color: '#000000' },
        bottom: { style: 'thick', color: '#FF0000' }
      }
      
      model.setCellBorder(0, 0, border)
      const result = model.getCellBorder(0, 0)
      
      expect(result?.top?.style).toBe('thin')
      expect(result?.bottom?.style).toBe('thick')
      expect(result?.bottom?.color).toBe('#FF0000')
    })

    it('应该合并边框设置', () => {
      const model = new SheetModel()
      
      model.setCellBorder(0, 0, { top: { style: 'thin' } })
      model.setCellBorder(0, 0, { bottom: { style: 'thick' } })
      
      const border = model.getCellBorder(0, 0)
      expect(border?.top?.style).toBe('thin')
      expect(border?.bottom?.style).toBe('thick')
    })

    it('应该清除单元格边框', () => {
      const model = new SheetModel()
      
      model.setCellBorder(0, 0, { top: { style: 'thin' } })
      model.clearCellBorder(0, 0)
      
      expect(model.getCellBorder(0, 0)).toBeUndefined()
    })
  })

  describe('范围操作', () => {
    it('应该设置范围全边框', () => {
      const model = new SheetModel()
      const border: CellBorder = {
        top: { style: 'thin' },
        right: { style: 'thin' },
        bottom: { style: 'thin' },
        left: { style: 'thin' }
      }
      
      model.setRangeBorder(0, 0, 2, 2, border)
      
      // 检查范围内所有单元格都有边框
      for (let r = 0; r <= 2; r++) {
        for (let c = 0; c <= 2; c++) {
          const cellBorder = model.getCellBorder(r, c)
          expect(cellBorder?.top?.style).toBe('thin')
          expect(cellBorder?.right?.style).toBe('thin')
          expect(cellBorder?.bottom?.style).toBe('thin')
          expect(cellBorder?.left?.style).toBe('thin')
        }
      }
    })

    it('应该设置范围外边框', () => {
      const model = new SheetModel()
      const edge: BorderEdge = { style: 'thick', color: '#000000' }
      
      model.setRangeOuterBorder(0, 0, 2, 2, edge)
      
      // 检查四角单元格有两条边框
      const topLeft = model.getCellBorder(0, 0)
      expect(topLeft?.top?.style).toBe('thick')
      expect(topLeft?.left?.style).toBe('thick')
      
      const bottomRight = model.getCellBorder(2, 2)
      expect(bottomRight?.bottom?.style).toBe('thick')
      expect(bottomRight?.right?.style).toBe('thick')
      
      // 检查中心单元格无边框
      expect(model.getCellBorder(1, 1)).toBeUndefined()
    })

    it('应该清除范围边框', () => {
      const model = new SheetModel()
      
      model.setRangeBorder(0, 0, 2, 2, {
        top: { style: 'thin' },
        right: { style: 'thin' },
        bottom: { style: 'thin' },
        left: { style: 'thin' }
      })
      
      model.clearRangeBorder(0, 0, 2, 2)
      
      for (let r = 0; r <= 2; r++) {
        for (let c = 0; c <= 2; c++) {
          expect(model.getCellBorder(r, c)).toBeUndefined()
        }
      }
    })
  })

  describe('边界情况', () => {
    it('应该处理部分边框', () => {
      const model = new SheetModel()
      
      model.setCellBorder(0, 0, { top: { style: 'thin' } })
      
      const border = model.getCellBorder(0, 0)
      expect(border?.top?.style).toBe('thin')
      expect(border?.right).toBeUndefined()
      expect(border?.bottom).toBeUndefined()
      expect(border?.left).toBeUndefined()
    })

    it('应该处理不存在的边框', () => {
      const model = new SheetModel()
      
      expect(model.getCellBorder(0, 0)).toBeUndefined()
    })

    it('应该支持所有边框样式', () => {
      const model = new SheetModel()
      const styles: BorderStyle[] = ['thin', 'medium', 'thick', 'dashed', 'dotted', 'double']
      
      styles.forEach((style, index) => {
        model.setCellBorder(index, 0, { top: { style } })
        expect(model.getCellBorder(index, 0)?.top?.style).toBe(style)
      })
    })
  })
})
```

### 2. 渲染测试

```typescript
describe('边框渲染', () => {
  it('应该调用正确的 Canvas API', () => {
    const canvas = document.createElement('canvas')
    const ctx = canvas.getContext('2d')!
    
    const spy = {
      strokeStyle: vi.spyOn(ctx, 'strokeStyle', 'set'),
      lineWidth: vi.spyOn(ctx, 'lineWidth', 'set'),
      stroke: vi.spyOn(ctx, 'stroke'),
      setLineDash: vi.spyOn(ctx, 'setLineDash')
    }

    const border: CellBorder = {
      top: { style: 'thin', color: '#FF0000' }
    }

    drawCellBorder(ctx, 0, 0, 100, 50, border)

    expect(spy.strokeStyle).toHaveBeenCalledWith('#FF0000')
    expect(spy.lineWidth).toHaveBeenCalledWith(1)
    expect(spy.stroke).toHaveBeenCalled()
  })

  it('应该处理虚线样式', () => {
    const canvas = document.createElement('canvas')
    const ctx = canvas.getContext('2d')!
    const spy = vi.spyOn(ctx, 'setLineDash')

    const border: CellBorder = {
      top: { style: 'dashed' }
    }

    drawCellBorder(ctx, 0, 0, 100, 50, border)

    expect(spy).toHaveBeenCalledWith([4, 2])
  })

  it('应该绘制双线', () => {
    const canvas = document.createElement('canvas')
    const ctx = canvas.getContext('2d')!
    const spy = vi.spyOn(ctx, 'stroke')

    const border: CellBorder = {
      top: { style: 'double' }
    }

    drawCellBorder(ctx, 0, 0, 100, 50, border)

    // 双线应该调用 stroke 两次
    expect(spy).toHaveBeenCalledTimes(2)
  })
})
```

---

## 📊 开发阶段

### 阶段 1: 数据结构 (Day 1, 0.5 天)

**目标**: 完成类型定义和数据存储

- [x] 定义 `BorderStyle` 类型
- [x] 定义 `BorderEdge` 接口
- [x] 定义 `CellBorder` 接口
- [x] 定义 `BORDER_PRESETS` 常量
- [x] 扩展 `SheetModel` 添加边框存储
- [x] 实现基础边框方法 (set/get/clear)

### 阶段 2: 渲染实现 (Day 1-2, 1 天)

**目标**: 完成 Canvas 边框绘制

- [ ] 实现 `drawBorderEdge()` 函数
- [ ] 实现 `drawDoubleBorder()` 函数
- [ ] 实现 `drawCellBorder()` 函数
- [ ] 集成到 `drawCells()` 函数
- [ ] 测试各种边框样式渲染
- [ ] 优化边框重叠处理

### 阶段 3: API 与集成 (Day 2, 0.5 天)

**目标**: 完成公共 API 和快捷方法

- [ ] 实现 `setRangeBorder()` 批量设置
- [ ] 实现 `setRangeOuterBorder()` 外边框
- [ ] 实现快捷方法 (allBorders/outerBorder/clearAll)
- [ ] 在 api.ts 暴露边框 API
- [ ] 更新 API 文档

### 阶段 4: 测试与优化 (Day 3, 1 天)

**目标**: 完整测试和性能优化

- [ ] 编写单元测试 (数据结构)
- [ ] 编写渲染测试 (Canvas API)
- [ ] 编写集成测试 (完整流程)
- [ ] 性能测试 (1000 单元格边框渲染)
- [ ] 优化边框绘制性能
- [ ] 代码审查和重构

---

## 🎯 验收标准

### 功能完整性
- [x] 支持 7 种边框样式 (none/thin/medium/thick/dashed/dotted/double)
- [x] 支持自定义颜色和宽度
- [x] 支持四边独立设置
- [x] 提供全边框、外边框、清除边框快捷方法
- [ ] 边框正确渲染到 Canvas
- [ ] 边框可以持久化保存

### 性能指标
- [ ] 1000 个边框单元格渲染 < 50ms
- [ ] 边框设置响应 < 5ms
- [ ] 无明显卡顿或延迟

### 代码质量
- [x] TypeScript 类型定义完整
- [ ] 单元测试覆盖率 > 80%
- [ ] 代码通过 TypeScript 编译
- [ ] 函数注释完整

### 用户体验
- [ ] 边框样式清晰可见
- [ ] 边框重叠处理正确
- [ ] 支持撤销重做
- [ ] API 易于使用

---

## 📝 使用示例

### 基础用法

```typescript
// 设置单个单元格边框
api.border.setCellBorder(0, 0, {
  top: { style: 'thin', color: '#000000' },
  bottom: { style: 'thick', color: '#FF0000' }
})

// 设置范围全边框
api.border.setAllBorders(0, 0, 5, 5, {
  style: 'thin',
  color: '#000000'
})

// 设置范围外边框
api.border.setOuterBorder(0, 0, 5, 5, {
  style: 'medium',
  color: '#0000FF'
})

// 清除边框
api.border.clearCellBorder(0, 0)
api.border.clearAllBorders(0, 0, 5, 5)
```

### 高级用法

```typescript
// 虚线边框
api.border.setCellBorder(0, 0, {
  top: { style: 'dashed', color: '#999999' },
  bottom: { style: 'dotted', color: '#999999' }
})

// 双线边框
api.border.setCellBorder(0, 0, {
  bottom: { style: 'double', color: '#000000', width: 3 }
})

// 自定义宽度
api.border.setCellBorder(0, 0, {
  left: { style: 'thin', color: '#FF0000', width: 2 }
})
```

---

## 🔄 后续扩展

### 短期 (下一版本)
- 边框 UI 工具栏组件
- 边框样式预览
- 更多预设边框样式

### 中期
- 对角线边框
- 图案填充边框
- Excel 边框格式导入导出

### 长期
- 智能边框（自动识别表格边界）
- 边框主题（预设配色方案）
- 边框动画效果

---

**文档创建**: 2025-11-27  
**最后更新**: 2025-11-27  
**维护者**: WorkfineSheet Team  
**状态**: ✅ 规划完成，开始实现
